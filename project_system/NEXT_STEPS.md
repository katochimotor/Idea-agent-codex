# Next Steps

1. Persist extracted `problems` and `problem_clusters` instead of keeping those stages in-memory only.
2. Add vector indexing for problems, clusters, and ideas so search can support more than document chunks.
3. Split the background worker into a separate process if job volume or reliability needs increase.
4. Add a project detail/view flow so created starter projects can be inspected from the UI.
